A "simple" app skeleton which uses React, React Router, Alt, SASS, and Webpack.

## Requirements
* Run `npm install` first to install dependencies

## Commands
* `npm run build` - Build the project
* `npm run watch` - Start the Webpack dev server
